
TODO
====

 * per module debugging information
 * implement a lock-free queue structure for the init queues (e.g. MS-queue)
(* provide a cache optimal local spinning lock in the abstracted threading module, e.g. ticket lock or MCS-lock)
 * aggregate modules into libraries with clear interface
 * doxygenify comments


In future:
----------

* write test settings
* evaluate performance

S-Net-porting:
* clarify sort_begin/sort_end injection in parallel combinator

and a lot more in farer future
... tdb
